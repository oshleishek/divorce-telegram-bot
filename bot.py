"""
Telegram Bot для лідогенерації адвокатів (Розлучення)
Версія: 3.1 ULTIMATE IMPROVED
Автор: Стас + Claude + Gemini

ЗМІНИ В v3.1 (ПОКРАЩЕНА ВОРОНКА):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Розширена сегментація: 4 → 10 сегментів (A1, A2, B1, B2, C1, C2, D1, D2, E1, E2)
✅ Детальні мині-кейси (реальні історії з іменами, містами, результатами)
✅ Мікрокоміти після кожної відповіді ("✅ Зрозуміло...")
✅ Живі тексти з емпатією та поясненнями
✅ Покращений оффер з реверс-ризиком та персоналізацією
✅ Покращені результати для кожного сегменту

ЗБЕРЕЖЕНО З v3.0:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✅ Аналітика конверсії (Analytics sheet)
✅ Всі користувачі (All_Users sheet)
✅ Ім'я з contact
✅ Нагадування про квіз (15 хв) та телефон (60 сек)
✅ Flask web-server для Render
✅ Тексти в константах
"""

import os
import logging
import random
from datetime import datetime
from telegram import Update, InlineKeyboardButton, InlineKeyboardMarkup, ReactionTypeEmoji
from telegram.ext import Application, CommandHandler, CallbackQueryHandler, MessageHandler, filters, ContextTypes
import gspread
from oauth2client.service_account import ServiceAccountCredentials
import requests
from flask import Flask
import threading
import asyncio
from telegram.constants import ChatAction

# =====================================================
# НАЛАШТУВАННЯ ЛОГУВАННЯ
# =====================================================

logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.INFO
)
logger = logging.getLogger(__name__)

# =====================================================
# КОНСТАНТИ
# =====================================================

BOT_TOKEN = os.environ.get('BOT_TOKEN', 'YOUR_BOT_TOKEN_HERE')
MAKE_WEBHOOK_URL = os.environ.get('MAKE_WEBHOOK_URL', '')
GOOGLE_SHEET_URL = os.environ.get('GOOGLE_SHEET_URL')

# =====================================================
# ПІДКЛЮЧЕННЯ ДО GOOGLE SHEETS
# =====================================================

def init_google_sheets():
    """Ініціалізація підключення до Google Sheets"""
    try:
        required_vars = [
            'GOOGLE_PROJECT_ID', 
            'GOOGLE_PRIVATE_KEY', 
            'GOOGLE_CLIENT_EMAIL',
            'GOOGLE_SHEET_URL'
        ]
        missing_vars = [var for var in required_vars if not os.environ.get(var)]
        
        if missing_vars:
            logger.warning(f"⚠️ Google Sheets не налаштовано (відсутні змінні: {', '.join(missing_vars)})")
            return None, None, None
        
        scope = ['https://spreadsheets.google.com/feeds',
                 'https://www.googleapis.com/auth/drive']
        
        creds_dict = {
            "type": "service_account",
            "project_id": os.environ.get('GOOGLE_PROJECT_ID'),
            "private_key_id": os.environ.get('GOOGLE_PRIVATE_KEY_ID'),
            "private_key": os.environ.get('GOOGLE_PRIVATE_KEY', '').replace('\\n', '\n'),
            "client_email": os.environ.get('GOOGLE_CLIENT_EMAIL'),
            "client_id": os.environ.get('GOOGLE_CLIENT_ID'),
            "auth_uri": "https://accounts.google.com/o/oauth2/auth",
            "token_uri": "https://oauth2.googleapis.com/token",
            "auth_provider_x509_cert_url": "https://www.googleapis.com/oauth2/v1/certs",
            "client_x509_cert_url": os.environ.get('GOOGLE_CERT_URL')
        }
        
        logger.info("🔄 Підключення до Google Sheets...")
        creds = ServiceAccountCredentials.from_json_keyfile_dict(creds_dict, scope)
        client = gspread.authorize(creds)
        
        logger.info(f"🔄 Відкриваю таблицю по URL...")
        spreadsheet = client.open_by_url(GOOGLE_SHEET_URL)
        
        # Отримуємо або створюємо листи
        try:
            leads_sheet = spreadsheet.worksheet("Leads")
        except gspread.WorksheetNotFound:
            leads_sheet = spreadsheet.add_worksheet("Leads", rows=1000, cols=20)
            # Додаємо заголовки (додали segment_name)
            leads_sheet.append_row([
                "Дата завершення", "Telegram ID", "Username", "Ім'я", "Телефон",
                "Діти", "Згода супруга", "Майно", "Місце супруга", "Терміновість",
                "Сегмент", "Назва сегменту", "Вартість", "Строки", "Статус"
            ])
        
        try:
            analytics_sheet = spreadsheet.worksheet("Analytics")
        except gspread.WorksheetNotFound:
            analytics_sheet = spreadsheet.add_worksheet("Analytics", rows=5000, cols=10)
            analytics_sheet.append_row([
                "Timestamp", "Telegram ID", "Username", "Event", "Details"
            ])
        
        try:
            all_users_sheet = spreadsheet.worksheet("All_Users")
        except gspread.WorksheetNotFound:
            all_users_sheet = spreadsheet.add_worksheet("All_Users", rows=5000, cols=10)
            all_users_sheet.append_row([
                "Дата першого контакту", "Telegram ID", "Username", 
                "First Name", "Last Name", "Завершив квіз", "Статус"
            ])
        
        logger.info(f"✅ Google Sheets підключено успішно")
        logger.info(f"  📊 Leads: {leads_sheet.title}")
        logger.info(f"  📈 Analytics: {analytics_sheet.title}")
        logger.info(f"  👥 All Users: {all_users_sheet.title}")
        
        return leads_sheet, analytics_sheet, all_users_sheet
        
    except Exception as e:
        logger.error(f"❌ Помилка підключення до Google Sheets: {type(e).__name__}: {str(e)}")
        return None, None, None

# Ініціалізуємо sheets
SHEETS_LEADS, SHEETS_ANALYTICS, SHEETS_ALL_USERS = init_google_sheets()

# =====================================================
# АНАЛІТИКА - ЛОГУВАННЯ ПОДІЙ
# =====================================================

async def log_event(telegram_id, username, event, details=""):
    """Логує кожну подію користувача для аналітики конверсії"""
    
    if SHEETS_ANALYTICS is None:
        return
    
    try:
        row = [
            datetime.now().isoformat(),
            str(telegram_id),
            username or "",
            event,
            details
        ]
        
        SHEETS_ANALYTICS.append_row(row)
        logger.info(f"📊 Analytics: {telegram_id} → {event}")
        
    except Exception as e:
        logger.error(f"❌ Помилка логування події: {e}")

async def save_all_user(telegram_id, username, first_name, last_name):
    """Зберігає ВСІХ користувачів, хто натиснув /start"""
    
    if SHEETS_ALL_USERS is None:
        return
    
    try:
        # Перевіряємо чи вже є такий користувач
        existing = SHEETS_ALL_USERS.find(str(telegram_id), in_column=2)
        if existing:
            logger.info(f"👥 Користувач {telegram_id} вже в базі")
            return
        
        row = [
            datetime.now().isoformat(),
            str(telegram_id),
            username or "",
            first_name or "",
            last_name or "",
            "Ні",  # Завершив квіз
            "new"   # Статус
        ]
        
        SHEETS_ALL_USERS.append_row(row)
        logger.info(f"👥 Новий користувач в базі: {username or telegram_id}")
        
    except Exception as e:
        logger.error(f"❌ Помилка збереження користувача: {e}")

# =====================================================
# WEB-СЕРВЕР ДЛЯ RENDER (ЩОБ НЕ ЗАСИНАВ)
# =====================================================

app = Flask(__name__)

@app.route('/')
def home():
    return "✅ Divorce Bot v3.1 is running!", 200

@app.route('/health')
def health():
    return {"status": "ok", "bot": "running", "version": "3.1"}, 200

def run_flask():
    """Запуск Flask в окремому потоці"""
    port = int(os.environ.get('PORT', 10000))
    app.run(host='0.0.0.0', port=port, debug=False, use_reloader=False)

# =====================================================
# РОЗШИРЕНА ЛОГІКА СЕГМЕНТАЦІЇ (10 СЕГМЕНТІВ)
# =====================================================

def determine_segment(user_data):
    """
    Визначає сегмент з 10 варіантів
    
    СЕГМЕНТИ:
    A1 - Експрес (немає дітей, є згода, немає майна, терміново)
    A2 - Простий (немає дітей, є згода, невелике майно/не терміново)
    B1 - Захист дітей (є діти, немає згоди)
    B2 - Мирна домовленість (є діти, є згода)
    C1 - Складний розділ (багато майна + діти)
    C2 - Економний розділ (є майно, немає дітей)
    D1 - Міжнародний VIP (за кордоном, терміново)
    D2 - Міжнародний стандарт (за кордоном, не терміново)
    E1 - Невідомо де супруг (не знають адресу, супруг згодний)
    E2 - Розшук супруга (не знають адресу, супруг проти)
    """
    
    has_children = user_data.get('has_children') == 'yes'
    spouse_consent = user_data.get('spouse_consent')
    property_dispute = user_data.get('property_dispute')
    spouse_location = user_data.get('spouse_location')
    urgency = user_data.get('urgency')
    
    # СЕГМЕНТ D1/D2: МІЖНАРОДНІ (перший пріоритет)
    if spouse_location == 'abroad':
        if urgency == 'high':
            return ('D1', '🌍 Міжнародне розлучення (VIP)', '12000-15000 грн', '3-4 місяці')
        else:
            return ('D2', '🌍 Міжнародне розлучення (Стандарт)', '9000-12000 грн', '3-4 місяці')
    
    # СЕГМЕНТ E1/E2: НЕВІДОМЕ МІСЦЕЗНАХОДЖЕННЯ
    if spouse_location == 'unknown':
        if spouse_consent == 'no':
            return ('E2', '🔍 Розлучення з розшуком супруга', '13000-16000 грн', '5-7 місяців')
        else:
            return ('E1', '🔍 Розлучення без адреси супруга', '10000-13000 грн', '4-6 місяців')
    
    # СЕГМЕНТ A1/A2: ШВИДКІ (немає дітей)
    if not has_children:
        if spouse_consent == 'yes' and property_dispute == 'no':
            if urgency == 'high':
                return ('A1', '⚡️ Експрес-розлучення', '4000-6000 грн', '2-3 місяці')
            else:
                return ('A2', '✅ Просте розлучення', '3500-5000 грн', '2-4 місяці')
        elif property_dispute == 'yes':
            return ('C2', '💰 Розділ майна', '12000-18000 грн', '5-8 місяців')
    
    # СЕГМЕНТ B1/B2: З ДІТЬМИ
    if has_children:
        if spouse_consent == 'no':
            return ('B1', '🛡 Захист інтересів дитини', '14000-18000 грн', '4-7 місяців')
        else:
            return ('B2', '👨‍👩‍👧 Розлучення зі згодою (з дітьми)', '10000-14000 грн', '3-5 місяців')
    
    # СЕГМЕНТ C1: СКЛАДНИЙ РОЗДІЛ
    if property_dispute == 'yes' and has_children:
        return ('C1', '💼 Складний розділ майна', '18000-30000 грн', '6-9 місяців')
    
    # ДЕФОЛТНИЙ
    return ('B2', '📋 Стандартне розлучення', '10000-12000 грн', '4-6 місяців')

# =====================================================
# 📝 ПОКРАЩЕНІ ТЕКСТИ ДЛЯ КОРИСТУВАЧА
# =====================================================

# 📝 ТЕКСТ: Привітання при /start (ПОКРАЩЕНО)
TEXT_WELCOME = """
👋 <b>Привіт!</b>

Я допоможу розібратися <b>скільки коштує і скільки триватиме саме ваше розлучення</b>.

Це займе <b>2 хвилини</b> і абсолютно <b>безкоштовно</b>.

<i>Кожна ситуація унікальна. Тому я задам кілька питань, щоб дати точний прогноз саме для вас.</i>

Готові почати?
"""

# 📝 ТЕКСТ: Питання 1 (ПОКРАЩЕНО з поясненням)
TEXT_Q1 = """
<b>Питання 1 з 6</b>

Чи є у вас спільні діти (до 18 років)?

<i>Це важливо, бо наявність дітей впливає на процедуру та строки розлучення.</i>
"""

# 📝 МІКРОКОМІТИ (нові тексти після відповідей)
MICROCOMMIT_Q1_YES = "✅ Зрозуміло, є діти.\n\n"
MICROCOMMIT_Q1_NO = "✅ Зрозуміло, дітей немає.\n\n"

# 📝 ТЕКСТ: Питання 2 (ПОКРАЩЕНО)
TEXT_Q2 = """<b>Питання 2 з 6</b>

Чи згоден(на) ваш чоловік/дружина на розлучення?

<i>Якщо обидва згодні - процес буде швидшим. Якщо хтось проти - це не перешкода, просто триватиме довше.</i>
"""

# 📝 МІКРОКОМІТИ для Q2
MICROCOMMIT_Q2_YES = "✅ Добре, що є згода.\n\n"
MICROCOMMIT_Q2_NO = "✅ Розумію, це складніше. Але розлучитися можна і без згоди.\n\n"
MICROCOMMIT_Q2_UNKNOWN = "✅ Добре, з'ясуємо далі.\n\n"

# 📝 ТЕКСТ: Питання 3 (ПОКРАЩЕНО)
TEXT_Q3 = """<b>Питання 3 з 6</b>

Чи є спільне майно, яке потрібно ділити?
<i>(квартира, будинок, земля, автомобіль, бізнес, великі заощадження тощо)</i>

<i>Якщо майна мало або ви домовилися - процес простіший. Якщо є великі активи - потрібен професійний розділ.</i>
"""

# 📝 МІКРОКОМІТИ для Q3
MICROCOMMIT_Q3_YES = "✅ Ага, розділ майна. Це вимагає особливої уваги.\n\n"
MICROCOMMIT_Q3_NO = "✅ Добре, майна немає або врегульовано.\n\n"
MICROCOMMIT_Q3_UNSURE = "✅ Зрозуміло, розберемося.\n\n"

# 📝 ТЕКСТ: Питання 4 (ПОКРАЩЕНО)
TEXT_Q4 = """<b>Питання 4 з 6</b>

Де зараз знаходиться ваш чоловік/дружина?

<i>Це впливає на те, як буде проходити розлучення: особисто в суді, онлайн або "за відсутності".</i>
"""

# 📝 МІКРОКОМІТИ для Q4
MICROCOMMIT_Q4_UKRAINE = "✅ Добре, супруг/а в Україні.\n\n"
MICROCOMMIT_Q4_ABROAD = "✅ Зрозуміло, за кордоном. Організуємо все онлайн.\n\n"
MICROCOMMIT_Q4_UNKNOWN = "✅ Розумію. Є процедура розлучення без адреси супруга.\n\n"

# 📝 ТЕКСТ: Питання 5 (ПОКРАЩЕНО)
TEXT_Q5 = """<b>Питання 5 з 6</b>

Наскільки терміново вам потрібно розлучення?

<i>Це допоможе підібрати оптимальний пакет послуг.</i>
"""

# 📝 ТЕКСТ: Питання 6 (ПОКРАЩЕНО)
TEXT_Q6_PHONE = """
✅ Дякую за відповіді!

<b>Останнє питання (6 з 6)</b>

Поділіться номером телефону, щоб отримати детальний розрахунок.

Це абсолютно <b>конфіденційно<b> і ні до чого вас не зобов'язує.

Ми не передаємо дані третім особам і не розсилаємо спам.

"""

# =====================================================
# ДЕТАЛЬНІ МИНІ-КЕЙСИ (ПОКРАЩЕНІ РЕАЛЬНІ ІСТОРІЇ)
# =====================================================

MINI_CASES = {
    # Для тих, у кого Є ДІТИ + НЕМАЄ ЗГОДИ
    'yes_children_no_consent': [
        """
━━━━━━━━━━━━━━━━━

💬 <b>Історія Марини (34 роки, Харків)</b>

<i>"Чоловік категорично був проти розлучення, погрожував забрати дитину. Я була в паніці.

Адвокат пояснила: в Україні дитина залишається з тим, з ким їй краще. Ми зібрали всі докази - я завжди займалася вихованням, водила до садка, до лікаря.

Через 5 місяців суд вирішив на мою користь. Донька зі мною, а колишній чоловік має право зустрічатися з нею по вихідних."</i>

✅ Результат: дитина залишилася з мамою, розлучення за 5 місяців

━━━━━━━━━━━━━━━━━

<i>Так, це реальна історія. Таких у нас десятки.

Давайте продовжимо ваш розрахунок...</i>
""",
        """
━━━━━━━━━━━━━━━━━

💬 <b>Історія Олега (38 років, Дніпро)</b>

<i>"Дружина подала на розлучення і хотіла обмежити моє спілкування з сином. Я боявся втратити дитину.

Адвокат допоміг скласти графік зустрічей, довів суду що я хороший батько. Збирали свідчення від вчителів, сусідів.

Зараз син живе з матір'ю, але я бачуся з ним 3 рази на тиждень і беру на канікули. Суд врахував мою позицію."</i>

✅ Результат: збережено повноцінне спілкування з дитиною

━━━━━━━━━━━━━━━━━

<i>Це реальні люди, реальні результати.

Продовжимо ваш розрахунок...</i>
"""
    ],
    
    # Для тих, у кого НЕМАЄ ДІТЕЙ + Є ЗГОДА
    'no_children_yes_consent': [
        """
━━━━━━━━━━━━━━━━━

💬 <b>Історія Оксани (33 роки, Київ)</b>

<i>"Ми з чоловіком зрозуміли, що розійшлися як люди. Без скандалів, просто різні шляхи. 

Найбільше я боялася, що розлучення затягнеться на рік і перетвориться на біль. Але адвокат все організував за 2.5 місяці. 

Я навіть в суд не ходила - адвокат представляв мої інтереси. Просто підписала документи і все."</i>

✅ Результат: розлучення за 2.5 місяці, витрати 4500 грн

━━━━━━━━━━━━━━━━━

<i>У вас схожа ситуація. Продовжимо розрахунок...</i>
""",
        """
━━━━━━━━━━━━━━━━━

💬 <b>Історія Андрія (29 років, Одеса)</b>

<i>"Коли ми з дружиною вирішили розлучитися, я думав треба йти в суд самому, заповнювати купу паперів...

Знайомий порекомендував адвоката. Вона за один день підготувала всі документи, все пояснила простою мовою.

Через 3 місяці я отримав рішення суду. Навіть не очікував, що може бути так просто."</i>

✅ Результат: розлучення за 3 місяці, без стресу

━━━━━━━━━━━━━━━━━

<i>Продовжимо ваш розрахунок...</i>
"""
    ],
    
    # Для тих, у кого Є МАЙНО
    'yes_property': [
        """
━━━━━━━━━━━━━━━━━

💬 <b>Історія Ірини та Сергія (Київ)</b>

<i>"У нас була квартира на двох, машина і дачна ділянка. Боялися, що суд розділить все 50/50 хоч я більше вклала грошей.

Адвокат знайшов спосіб: ми довели що частина грошей на квартиру були від моїх батьків (подарунок). В результаті я отримала більшу частку.

Це зайняло 7 місяців, але воно того варте - зберегла свою квартиру."</i>

✅ Результат: справедливий розділ з урахуванням внесків

━━━━━━━━━━━━━━━━━

<i>Розділ майна - складне питання. Продовжимо розрахунок...</i>
"""
    ],
    
    # Для тих, хто ЗА КОРДОНОМ
    'abroad': [
        """
━━━━━━━━━━━━━━━━━

💬 <b>Історія Яни (30 років, зараз в Німеччині)</b>

<i>"Я виїхала в Німеччину з дітьми, а чоловік залишився в Україні. Думала доведеться повертатися для розлучення.

Адвокат організував все онлайн: я оформила довіреність в консульстві, надіслала документи поштою.

Через 4 місяці отримала рішення суду поштою до Берліна. Жодного разу не приїжджала."</i>

✅ Результат: розлучення повністю онлайн, без приїзду

━━━━━━━━━━━━━━━━━

<i>Ви за кордоном? Не проблема. Продовжимо...</i>
"""
    ],
    
    # Для тих, хто НЕ ЗНАЄ ДЕ СУПРУГ
    'unknown_location': [
        """
━━━━━━━━━━━━━━━━━

💬 <b>Історія Олени (29 років, Запоріжжя)</b>

<i>"Чоловік поїхав на заробітки в Польщу і пропав. Телефон не бере, адреси не знаю.

Думала не можу розлучитися без нього. Але адвокат пояснив: можна через оголошення в пресі та на сайті суду.

Через 6 місяців я отримала рішення про розлучення. Чоловік так і не з'явився, але процес відбувся без нього."</i>

✅ Результат: розлучення без участі супруга, через 6 місяців

━━━━━━━━━━━━━━━━━

<i>Не знаєте де супруг? Це не перешкода. Продовжимо...</i>
"""
    ],
    
    # ЗАГАЛЬНИЙ (якщо нічого не підійшло)
    'default': [
        """
━━━━━━━━━━━━━━━━━

💬 <b>Що кажуть наші клієнти:</b>

<i>"Найбільше боялася що розлучення перетвориться на нескінченну тяганину. 

Але адвокат з першого дня пояснила весь план, що і коли буде відбуватися. Це дуже заспокоїло.

Зараз все позаду. Дякую що були поруч."</i>

— Тетяна, 31 рік, Вінниця

━━━━━━━━━━━━━━━━━

<i>У нас великий досвід. Продовжимо ваш розрахунок...</i>
"""
    ]
}

def get_mini_case(user_data):
    """Вибирає релевантний детальний мині-кейс"""
    
    has_children = user_data.get('has_children') == 'yes'
    spouse_consent = user_data.get('spouse_consent')
    property_dispute = user_data.get('property_dispute')
    spouse_location = user_data.get('spouse_location')
    
    # Пріоритет 1: За кордоном
    if spouse_location == 'abroad':
        cases = MINI_CASES['abroad']
        
    # Пріоритет 2: Невідоме місце
    elif spouse_location == 'unknown':
        cases = MINI_CASES['unknown_location']
        
    # Пріоритет 3: Є майно
    elif property_dispute == 'yes':
        cases = MINI_CASES['yes_property']
        
    # Пріоритет 4: Є діти + немає згоди
    elif has_children and spouse_consent == 'no':
        cases = MINI_CASES['yes_children_no_consent']
        
    # Пріоритет 5: Немає дітей + є згода
    elif not has_children and spouse_consent == 'yes':
        cases = MINI_CASES['no_children_yes_consent']
        
    # Дефолтний
    else:
        cases = MINI_CASES['default']
    
    return random.choice(cases)

# =====================================================
# ПОКРАЩЕНІ РЕЗУЛЬТАТИ ДЛЯ 10 СЕГМЕНТІВ
# =====================================================

SEGMENT_MESSAGES = {
    'A1': """
⚡️ <b>{segment_name}</b>

Відмінна новина! У вас найпростіша ситуація - можемо розлучити швидко і без зайвих витрат.

💰 <b>Вартість:</b> {cost}
⏱ <b>Строки:</b> {time}

<b>Чому саме так:</b>
• Немає дітей → немає питань про опіку
• Супруг/а згодні → не буде затягувань
• Немає спорів про майно → все просто
• Ви хочете терміново → пріоритет

<b>Що входить:</b>
✅ Підготовка позовної заяви за 1 день
✅ Подання до суду
✅ Представництво на засіданні (можете не приходити)
✅ Отримання рішення суду

<i>Це ваш "зелений коридор" - найшвидший спосіб розлучення.</i>
""",

    'A2': """
✅ <b>{segment_name}</b>

Хороші новини! Ваша ситуація цілком стандартна і прогнозована.

💰 <b>Вартість:</b> {cost}
⏱ <b>Строки:</b> {time}

<b>Особливості вашого випадку:</b>
• Немає дітей → спрощена процедура
• Є згода супруга → швидше
• Майно врегульовано або його мало

<b>Що входить:</b>
✅ Вся документація "під ключ"
✅ Подання до суду
✅ Представництво на засіданнях
✅ Рішення суду на руки

<i>Оскільки немає складнощів, процес пройде спокійно і без стресу.</i>
""",

    'B1': """
🛡 <b>{segment_name}</b>

Розумію, наскільки це складно. Але у нас великий досвід саме таких справ.

💰 <b>Вартість:</b> {cost}
⏱ <b>Строки:</b> {time}

<b>Чому потрібен адвокат саме тут:</b>
• Є діти → треба визначити місце проживання
• Супруг проти → потрібна чітка стратегія
• Важливо захистити права дитини

<b>Що ми робимо:</b>
✅ Складаємо стратегію захисту ваших інтересів
✅ Готуємо докази вашої прихильності до дитини
✅ Представляємо інтереси на всіх засіданнях
✅ Домагаємося справедливого графіка зустрічей
✅ Призначення аліментів

<i>Головне - захистити дитину від стресу і зберегти ваш зв'язок з нею. У нас це виходить.</i>
""",

    'B2': """
👨‍👩‍👧 <b>{segment_name}</b>

Дуже добре, що ви домовилися. Це найкраще для дитини.

💰 <b>Вартість:</b> {cost}
⏱ <b>Строки:</b> {time}

<b>Ваша ситуація:</b>
• Є діти, але ви домовилися про опіку
• Обидва хочете швидко і без конфліктів
• Потрібно оформити все юридично правильно

<b>Що ми зробимо:</b>
✅ Оформимо угоду про місце проживання дитини
✅ Зафіксуємо домовленості про аліменти
✅ Подамо все до суду одним пакетом
✅ Проведемо через суд швидко

<i>Оскільки є згода, процес буде набагато швидшим. Головне - правильно все оформити.</i>
""",

    'C1': """
💼 <b>{segment_name}</b>

Ваш випадок вимагає високої кваліфікації. Це серйозна робота.

💰 <b>Вартість:</b> {cost}
⏱ <b>Строки:</b> {time}

<b>Чому це складно:</b>
• Багато спільного майна → потрібна оцінка
• Є діти → подвійна складність
• Можливі спроби приховати активи

<b>Наша робота:</b>
✅ Повний аудит спільного майна
✅ Залучення експертів-оцінювачів
✅ Виявлення прихованих активів
✅ Розробка стратегії справедливого розділу
✅ Представництво на всіх засіданнях
✅ Захист інтересів дитини

<i>Без професійного адвоката можна втратити значну частину активів. Ми цього не допустимо.</i>
""",

    'C2': """
💰 <b>{segment_name}</b>

Маєте спільне майно - це вимагає правильного підходу.

💰 <b>Вартість:</b> {cost}
⏱ <b>Строки:</b> {time}

<b>Особливості:</b>
• Є спільне майно для розділу
• Потрібно оцінити і розподілити справедливо
• Важливо не втратити своє

<b>Що входить:</b>
✅ Інвентаризація спільного майна
✅ Оцінка вартості (за потреби)
✅ Пошук компромісних варіантів
✅ Представництво в суді
✅ Захист ваших інтересів

<i>Розділ майна - завжди делікатне питання. Ми знаємо як домовитися або відстояти вашу позицію в суді.</i>
""",

    'D1': """
🌍 <b>{segment_name}</b>

Відстань - не перешкода. Ви хочете швидко - ми організуємо.

💰 <b>Вартість:</b> {cost}
⏱ <b>Строки:</b> {time}

<b>Ваша ситуація:</b>
• Один з подружжя за кордоном
• Потрібно терміново
• Хочете VIP-рівень сервісу

<b>Що ми робимо:</b>
✅ Повна організація процесу онлайн
✅ Підготовка довіреності (консультуємо як оформити)
✅ Представництво без вашої присутності
✅ Всі документи надсилаємо поштою за кордон
✅ Постійна комунікація (Telegram/WhatsApp)
✅ Пріоритетний розгляд

<i>Ви можете бути в будь-якій країні - ми все зробимо за вас. Навіть не треба приїжджати.</i>
""",

    'D2': """
🌍 <b>{segment_name}</b>

Перебуваєте за кордоном? Не проблема - організуємо все дистанційно.

💰 <b>Вартість:</b> {cost}
⏱ <b>Строки:</b> {time}

<b>Особливості:</b>
• Один/обидва за кордоном
• Розлучення без приїзду в Україну
• Вся комунікація онлайн

<b>Як це працює:</b>
✅ Ви оформляєте довіреність в консульстві
✅ Надсилаєте документи поштою/скан-копіями
✅ Ми представляємо ваші інтереси в суді
✅ Рішення суду отримуєте поштою за кордон
✅ Весь процес - через месенджери

<i>Тисячі українців розлучилися будучи за кордоном. Це стандартна процедура для нас.</i>
""",

    'E1': """
🔍 <b>{segment_name}</b>

Не знаєте де супруг/а - це не перешкода для розлучення.

💰 <b>Вартість:</b> {cost}
⏱ <b>Строки:</b> {time}

<b>Чому це можливо:</b>
• Закон дозволяє розлучення "за відсутності"
• Через оголошення в пресі та на сайті суду
• Процес триває довше, але він легальний

<b>Що ми робимо:</b>
✅ Публікуємо оголошення про розлучення
✅ Надсилаємо повістки на останню адресу
✅ Представляємо ваші інтереси в суді
✅ Доводимо, що ви намагалися повідомити супруга
✅ Отримуємо рішення суду

<i>Навіть якщо супруг/а пропав/ла безвісти - ви маєте право на нове життя. Допоможемо.</i>
""",

    'E2': """
🔍 <b>{segment_name}</b>

Супруг проти і може уникати - знаємо як з цим працювати.

💰 <b>Вартість:</b> {cost}
⏱ <b>Строки:</b> {time}

<b>Ваша ситуація:</b>
• Супруг проти розлучення
• Не знаєте де він зараз / може ховатися
• Боїтеся що процес затягнеться

<b>Наша тактика:</b>
✅ Спроба встановити місцезнаходження
✅ Офіційні запити за останньою адресою
✅ Публікація оголошень
✅ Судові засідання "за відсутності"
✅ Захист ваших інтересів

<i>Важливо: суд не може відмовити вам у розлученні через те, що супруг ховається. Ми це знаємо і використовуємо.</i>
"""
}

# ЗАМІНИ ЦЮ ФУНКЦІЮ ПОВНІСТЮ
def get_personalized_offer(first_name, segment_name):
    """Генерує персоналізований оффер (ПРОДАЄМО РЕЗУЛЬТАТ)"""
    
    # Видаляємо емодзі, якщо він є в назві сегменту
    cleaned_segment_name = segment_name.split(' ', 1)[-1]
    
    return f"""
🎁 <b>{first_name}, У МЕНЕ ДЛЯ ВАС СПЕЦІАЛЬНА ПРОПОЗИЦІЯ</b>

Ви отримуєте не просто "консультацію". Ви купуєте <b>результат: повну ясність і контроль над ситуацією.</b>

Зазвичай розробка такого покрокового плану коштує 2000 грн.

Але для тих, хто пройшов тест, ціна — <b>всього 990 грн</b> ⏰ <i>(діє 24 години)</i>

<b>ЩО КОНКРЕТНО ВИ ОТРИМАЄТЕ (РЕЗУЛЬТАТИ):</b>

<b>✅ 1. ПОВНА ЯСНІСТЬ</b>
Ми перетворимо ваш "хаос" і паніку на чіткий, зрозумілий процес. Ви будете точно знати, що, як і в якому порядку робити.

<b>✅ 2. ПОКРОКОВИЙ ПЛАН (під ваш випадок)</b>
Ви отримаєте персональну "дорожню карту" саме для вашої ситуації (<b>{cleaned_segment_name}</b>), яка крок за кроком приведе вас до мети.

<b>✅ 3. ЗАХИСТ ВІД ПОМИЛОК</b>
Ми покажемо, де 90% людей втрачають гроші та час. Ви уникнете цих помилок і збережете свої активи, нерви та стосунки з дітьми.

<b>✅ 4. КОНТРОЛЬ НАД СИТУАЦІЄЮ</b>
Найголовніше. Страх і невизначеність зникнуть. Ви підете з розмови з відчуттям впевненості та повного контролю над своїм життям.

<b>🎯 НАЙГОЛОВНІШЕ (ЗНЯТТЯ РИЗИКУ):</b>
Якщо після консультації ви вирішите працювати з нами - ці 990 грн <b>ПОВНІСТЮ ЗАРАХОВУЮТЬСЯ</b> в оплату основної послуги!

<i>Тобто, по суті, ви нічим не ризикуєте.</i>
"""

# 📝 ТЕКСТ: Підтвердження запису (ПОКРАЩЕНО)
def get_consultation_booked_text(first_name, phone):
    """Генерує текст підтвердження з конфеті"""
    return f"""
✅ <b>Чудово, {first_name}!</b>

Ваш запит прийнято.

Наш адвокат <b>зв'яжеться з вами протягом 5-15 хвилин</b>, щоб узгодити зручний час консультації.

<i>Очікуйте дзвінок на номер:</i> <code>{phone}</code>

Якщо не зможемо дотелефонуватись, напишемо вам сюди в Telegram.

<b>Дякуємо за довіру!</b> 🙏

<i>P.S. Підготуйтеся до консультації: подумайте які питання хочете обговорити і які документи маєте під рукою.</i>
"""

# 📝 ІНШІ ТЕКСТИ (незмінні)
TEXT_UNKNOWN_MESSAGE = "Вибачте, не розумію 🤔\n\nНатисніть /start, щоб почати розрахунок."

TEXT_PHONE_REMINDER = """
👋 Здається, ви не натиснули кнопку...

Щоб отримати ваш персональний розрахунок, будь ласка, натисніть кнопку «📱 Поділитися номером».

Це абсолютно безпечно і потрібно лише для того, щоб відправити вам результат.
"""

TEXT_QUIZ_REMINDER = """
👋 Здається, ви відволіклися...

Ми зупинилися на півдорозі до розрахунку вартості. Бажаєте продовжити?

Просто натисніть /start, щоб почати заново (це швидко!), або дайте відповідь на останнє запитання, якщо воно ще на екрані.
"""

# =====================================================
# ОБРОБНИКИ КОМАНД
# =====================================================

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обробник команди /start"""
    
    user = update.effective_user

    await remove_quiz_reminder(context, user.id)
    
    # Зберігаємо користувача в базу "All Users"
    await save_all_user(
        telegram_id=user.id,
        username=user.username,
        first_name=user.first_name,
        last_name=user.last_name
    )
    
    await log_event(user.id, user.username, "/start", "Користувач почав взаємодію")
    
    # Ініціалізуємо дані
    context.user_data.clear()
    context.user_data['telegram_id'] = user.id
    context.user_data['username'] = user.username or ''
    context.user_data['started_at'] = datetime.now().isoformat()
    
    keyboard = [[InlineKeyboardButton("✅ Так, почнемо!", callback_data='start_quiz')]]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await update.message.reply_text(
        TEXT_WELCOME,
        parse_mode='HTML',
        reply_markup=reply_markup
    )

# =====================================================
# ОБРОБНИКИ КВІЗУ (ПОКРАЩЕНІ З МІКРОКОМІТАМИ)
# =====================================================

async def question_1(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Q1: Чи є діти?"""
    query = update.callback_query
    await query.answer()
    
    user_id = update.effective_user.id
    username = update.effective_user.username
    
    await log_event(user_id, username, "quiz_started", "Користувач почав квіз")
    
    keyboard = [
        [InlineKeyboardButton("👶 Так, є діти", callback_data='q1_yes')],
        [InlineKeyboardButton("❌ Немає дітей", callback_data='q1_no')]
    ]
    
    await query.edit_message_text(
        TEXT_Q1,
        parse_mode='HTML',
        reply_markup=InlineKeyboardMarkup(keyboard)
    )
    
    await schedule_quiz_reminder(context, user_id, query.message.chat_id)

async def question_2(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Q2: Згода супруга (З МІКРОКОМІТОМ)"""
    query = update.callback_query
    await query.answer()
    
    user_id = update.effective_user.id
    username = update.effective_user.username
    
    # Зберігаємо відповідь
    answer = 'yes' if query.data == 'q1_yes' else 'no'
    context.user_data['has_children'] = answer
    
    await log_event(user_id, username, "q1_answered", f"has_children={answer}")
    
    # МІКРОКОМІТ (підтвердження)
    microcommit = MICROCOMMIT_Q1_YES if answer == 'yes' else MICROCOMMIT_Q1_NO
    
    text = microcommit + TEXT_Q2
    
    keyboard = [
        [InlineKeyboardButton("✅ Так, згоден/на", callback_data='q2_yes')],
        [InlineKeyboardButton("❌ Ні, категорично проти", callback_data='q2_no')],
        [InlineKeyboardButton("🤷 Не знаю / не впевнений/а", callback_data='q2_unknown')]
    ]
    
    await query.edit_message_text(
        text,
        parse_mode='HTML',
        reply_markup=InlineKeyboardMarkup(keyboard)
    )
    
    await schedule_quiz_reminder(context, user_id, query.message.chat_id)

async def question_3(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Q3: Розділ майна (З МІКРОКОМІТОМ)"""
    query = update.callback_query
    await query.answer()
    
    user_id = update.effective_user.id
    username = update.effective_user.username
    
    # Зберігаємо відповідь
    consent = query.data.replace('q2_', '')
    context.user_data['spouse_consent'] = consent
    
    await log_event(user_id, username, "q2_answered", f"spouse_consent={consent}")
    
    # МІКРОКОМІТ
    if consent == 'yes':
        microcommit = MICROCOMMIT_Q2_YES
    elif consent == 'no':
        microcommit = MICROCOMMIT_Q2_NO
    else:
        microcommit = MICROCOMMIT_Q2_UNKNOWN
    
    text = microcommit + TEXT_Q3
    
    keyboard = [
        [InlineKeyboardButton("✅ Так, є майно для розділу", callback_data='q3_yes')],
        [InlineKeyboardButton("❌ Немає / все врегульовано", callback_data='q3_no')],
        [InlineKeyboardButton("🤷 Не впевнений/а", callback_data='q3_unsure')]
    ]
    
    await query.edit_message_text(
        text,
        parse_mode='HTML',
        reply_markup=InlineKeyboardMarkup(keyboard)
    )
    
    await schedule_quiz_reminder(context, user_id, query.message.chat_id)

# ЗАМІНИ ЦЮ ФУНКЦІЮ ПОВНІСТЮ
async def question_4(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Q4: Місце супруга (З МІНІ-КЕЙСОМ ТА МІКРОКОМІТОМ)"""
    query = update.callback_query
    await query.answer()
    
    user_id = update.effective_user.id
    username = update.effective_user.username
    chat_id = query.message.chat_id
    
    # Зберігаємо відповідь на Q3
    property = query.data.replace('q3_', '')
    context.user_data['property_dispute'] = property
    
    await log_event(user_id, username, "q3_answered", f"property_dispute={property}")
    
    # МІКРОКОМІТ
    if property == 'yes':
        microcommit = MICROCOMMIT_Q3_YES
    elif property == 'no':
        microcommit = MICROCOMMIT_Q3_NO
    else:
        microcommit = MICROCOMMIT_Q3_UNSURE
    
    # Отримуємо детальний мині-кейс
    mini_case = get_mini_case(context.user_data)
    
    # <<< НОВЕ: Спочатку відправляємо тільки мікро-комміт (редагуємо старе повідомлення)
    await query.edit_message_text(
        microcommit,
        parse_mode='HTML'
    )
    
    # <<< НОВЕ: Анонсуємо кейс (новим повідомленням)
    await asyncio.sleep(1)
    await context.bot.send_chat_action(chat_id=chat_id, action=ChatAction.TYPING)
    await context.bot.send_message(
        chat_id=chat_id,
        text="<b>Хвилинку...<b> Ваша ситуація дуже схожа на реальний випадок, який ми вели минулого місяця. Подивіться:",
        parse_mode='HTML'
    )
    
    # <<< НОВЕ: Імітуємо друк та відправляємо сам кейс (новим повідомленням)
    await asyncio.sleep(1.5)
    await context.bot.send_chat_action(chat_id=chat_id, action=ChatAction.TYPING)
    
    await context.bot.send_message(
        chat_id=chat_id,
        text=mini_case,
        parse_mode='HTML'
    )
    
    # <<< НОВЕ: Пауза, щоб дати час прочитати, та відправляємо Q4
    await asyncio.sleep(4)
    await context.bot.send_chat_action(chat_id=chat_id, action=ChatAction.TYPING)

    # Клавіатура для Q4 (з твого оригінального коду)
    keyboard_q4 = [
        [InlineKeyboardButton("🇺🇦 В Україні (знаю адресу)", callback_data='q4_ukraine')],
        [InlineKeyboardButton("✈️ За кордоном", callback_data='q4_abroad')],
        [InlineKeyboardButton("❓ Не знаю де / пропав/ла", callback_data='q4_unknown')]
    ]
    reply_markup_q4 = InlineKeyboardMarkup(keyboard_q4)

    # Відправляємо Q4 (новим повідомленням)
    await context.bot.send_message(
        chat_id=chat_id,
        text=TEXT_Q4,
        parse_mode='HTML',
        reply_markup=reply_markup_q4
    )
    
    await schedule_quiz_reminder(context, user_id, chat_id)

async def question_5(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Q5: Терміновість (З МІКРОКОМІТОМ)"""
    query = update.callback_query
    await query.answer()
    
    user_id = update.effective_user.id
    username = update.effective_user.username
    
    # Зберігаємо відповідь
    location = query.data.replace('q4_', '')
    context.user_data['spouse_location'] = location
    
    await log_event(user_id, username, "q4_answered", f"spouse_location={location}")
    
    # МІКРОКОМІТ
    if location == 'ukraine':
        microcommit = MICROCOMMIT_Q4_UKRAINE
    elif location == 'abroad':
        microcommit = MICROCOMMIT_Q4_ABROAD
    else:
        microcommit = MICROCOMMIT_Q4_UNKNOWN
    
    text = microcommit + TEXT_Q5
    
    keyboard = [
        [InlineKeyboardButton("⚡️ Дуже терміново (2-3 міс)", callback_data='q5_high')],
        [InlineKeyboardButton("⏳ Можу почекати (4-6 міс)", callback_data='q5_medium')],
        [InlineKeyboardButton("🤷 Не критично", callback_data='q5_low')]
    ]
    
    await query.edit_message_text(
        text,
        parse_mode='HTML',
        reply_markup=InlineKeyboardMarkup(keyboard)
    )
    
    await schedule_quiz_reminder(context, user_id, query.message.chat_id)

async def question_6_phone(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Q6: Запит номера телефону"""
    query = update.callback_query
    await query.answer()
    
    user_id = update.effective_user.id
    username = update.effective_user.username
    
    # Зберігаємо відповідь
    urgency = query.data.replace('q5_', '')
    context.user_data['urgency'] = urgency
    
    await log_event(user_id, username, "q5_answered", f"urgency={urgency}")

    await remove_quiz_reminder(context, user_id)
    
    from telegram import KeyboardButton, ReplyKeyboardMarkup
    
    keyboard = [[KeyboardButton("📱 Поділитися номером", request_contact=True)]]
    reply_markup = ReplyKeyboardMarkup(keyboard, one_time_keyboard=True, resize_keyboard=True)
    
    await query.edit_message_text(TEXT_Q6_PHONE, parse_mode='HTML')
    
    # Відправляємо окреме повідомлення з кнопкою
    await context.bot.send_message(
        chat_id=query.message.chat_id,
        text="👇 Натисніть кнопку нижче:",
        reply_markup=reply_markup
    )

    chat_id = query.message.chat_id
    
    # Запускаємо таймер на 60 секунд
    context.job_queue.run_once(
        phone_reminder_callback,
        60,
        chat_id=chat_id,
        user_id=user_id,
    )

# ЗАМІНИ ЦЮ ФУНКЦІЮ ПОВНІСТЮ
async def process_contact(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обробка номера телефону та завершення квізу"""
    
    contact = update.message.contact
    user = update.effective_user
    chat_id = update.message.chat_id # <<< НОВЕ: Отримуємо chat_id
    
    # Отримуємо ім'я з contact
    first_name = contact.first_name or user.first_name or "Клієнт"
    last_name = contact.last_name or user.last_name or ""
    
    context.user_data['first_name'] = first_name
    context.user_data['last_name'] = last_name
    context.user_data['phone_number'] = contact.phone_number
    context.user_data['completed_at'] = datetime.now().isoformat()
    
    user_id = user.id
    username = user.username
    
    await log_event(user_id, username, "phone_shared", f"{first_name} - {contact.phone_number}")
    
    # Оновлюємо статус в All_Users
    if SHEETS_ALL_USERS:
        try:
            cell = SHEETS_ALL_USERS.find(str(user_id), in_column=2)
            if cell:
                SHEETS_ALL_USERS.update_cell(cell.row, 6, "Так")
        except:
            pass
    
    # Визначаємо сегмент (РОЗШИРЕНА ЛОГІКА)
    segment, segment_name, cost, time = determine_segment(context.user_data)
    context.user_data['segment'] = segment
    context.user_data['segment_name'] = segment_name
    context.user_data['cost_estimate'] = cost
    context.user_data['time_estimate'] = time
    context.user_data['status'] = 'new' # <<< ВАЖЛИВО: Встановлюємо початковий статус
    
    logger.info(f"📊 Новий лід: {first_name} ({segment} - {segment_name})")
    
    # Зберігаємо в Sheets
    await save_to_sheets(context.user_data)
    await send_to_make(context.user_data)
    
    # Подяка перед результатом
    thanks_text = f"""
✅ <b>Дякую, {first_name}!</b>

Зараз я розрахую вартість і строки саме для вашої ситуації...
"""
    
    from telegram import ReplyKeyboardRemove
    await update.message.reply_text(
        thanks_text,
        parse_mode='HTML',
        reply_markup=ReplyKeyboardRemove()
    )
    
    # Пауза (імітація розрахунку)
    await asyncio.sleep(2)
    
    # Відправляємо результат
    await send_result(update, context, segment, segment_name, cost, time)
    
    # Пауза перед оффером
    await asyncio.sleep(3)
    
    # Відправляємо персоналізований оффер
    await send_first_offer(update, context, first_name)
    
    # <<< НОВЕ: ПЛАНУЄМО НАГАДУВАННЯ (Фіча #3)
    # Через 2 години (7200 сек)
    job_name = f"offer_reminder_{user_id}"
    context.job_queue.run_once(
        offer_reminder_callback,
        7200, 
        chat_id=chat_id,
        user_id=user_id,
        name=job_name,
        data=first_name  # Передаємо ім'я в функцію
    )
    logger.info(f"⏰ Заплановано нагадування про оффер для {user_id} через 2 години")

async def save_to_sheets(user_data):
    """Зберігає дані ліда в Google Sheets"""
    
    if SHEETS_LEADS is None:
        logger.warning("⚠️ Google Sheets не підключено")
        return
    
    try:
        row = [
            user_data.get('completed_at', ''),
            str(user_data.get('telegram_id', '')),
            user_data.get('username', ''),
            user_data.get('first_name', ''),
            user_data.get('phone_number', ''),
            user_data.get('has_children', ''),
            user_data.get('spouse_consent', ''),
            user_data.get('property_dispute', ''),
            user_data.get('spouse_location', ''),
            user_data.get('urgency', ''),
            user_data.get('segment', ''),
            user_data.get('segment_name', ''),  # ДОДАНО
            user_data.get('cost_estimate', ''),
            user_data.get('time_estimate', ''),
            user_data.get('status', 'new')
        ]
        
        SHEETS_LEADS.append_row(row)
        logger.info(f"✅ Лід збережено: {user_data.get('first_name')}")
        
    except Exception as e:
        logger.error(f"❌ Помилка збереження: {e}")

async def send_to_make(user_data):
    """Відправляє webhook в Make.com"""
    
    if not MAKE_WEBHOOK_URL:
        return
    
    try:
        payload = {
            'event': 'new_lead',
            'telegram_id': user_data.get('telegram_id'),
            'first_name': user_data.get('first_name'),
            'phone_number': user_data.get('phone_number'),
            'segment': user_data.get('segment'),
            'segment_name': user_data.get('segment_name'),
            'cost_estimate': user_data.get('cost_estimate'),
            'time_estimate': user_data.get('time_estimate'),
            'completed_at': user_data.get('completed_at')
        }
        
        response = requests.post(MAKE_WEBHOOK_URL, json=payload, timeout=5)
        
        if response.status_code == 200:
            logger.info("✅ Webhook відправлено")
            
    except Exception as e:
        logger.error(f"❌ Помилка webhook: {e}")

async def send_result(update: Update, context: ContextTypes.DEFAULT_TYPE, segment, segment_name, cost, time):
    """Відправляє персоналізований результат"""
    
    message_template = SEGMENT_MESSAGES.get(segment, SEGMENT_MESSAGES['B2'])
    result_text = message_template.format(
        segment_name=segment_name,
        cost=cost, 
        time=time
    )
    
    await update.message.reply_text(
        result_text,
        parse_mode='HTML'
    )

# ЗАМІНИ ЦЮ ФУНКЦІЮ ПОВНІСТЮ
async def send_first_offer(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Відправляє персоналізований оффер (з назвою сегменту)"""
    
    # Отримуємо ім'я та сегмент з user_data
    first_name = context.user_data.get('first_name', 'Клієнт')
    segment_name = context.user_data.get('segment_name', 'Ваш випадок') # Отримуємо назву сегменту
    
    # Генеруємо новий оффер, передаючи туди ім'я та сегмент
    offer_text = get_personalized_offer(first_name, segment_name)
    
    keyboard = [
        [InlineKeyboardButton("📅 Отримати Ясність і План (990 грн)", callback_data='book_consultation')]
    ]
    reply_markup = InlineKeyboardMarkup(keyboard)
    
    await update.message.reply_text(
        offer_text,
        parse_mode='HTML',
        reply_markup=reply_markup
    )

async def book_consultation(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обробка запису на консультацію (З КОНФЕТІ та бонусом)"""
    
    query = update.callback_query
    await query.answer()
    
    user_data = context.user_data
    user_id = update.effective_user.id
    username = update.effective_user.username
    first_name = user_data.get('first_name', 'Клієнт')

    # <<< НОВЕ: СКАСОВУЄМО НАГАДУВАННЯ (Фіча #3)
    job_name = f"offer_reminder_{user_id}"
    current_jobs = context.job_queue.get_jobs_by_name(job_name)
    if current_jobs:
        for job in current_jobs:
            job.schedule_removal()
        logger.info(f"⏰ Видалено нагадування про оффер для {user_id} (юзер записався)")
    
    # <<< НОВЕ: Оновлюємо статус в user_data (для логіки колбеку)
    context.user_data['status'] = 'scheduled'
    
    logger.info(f"🔥 ГАРЯЧИЙ ЛІД! {first_name} хоче консультацію!")
    
    await log_event(user_id, username, "consultation_booked", "Запис на консультацію!")
    
    # Оновлюємо статус в All_Users
    if SHEETS_ALL_USERS:
        try:
            cell = SHEETS_ALL_USERS.find(str(user_id), in_column=2)
            if cell:
                SHEETS_ALL_USERS.update_cell(cell.row, 7, "scheduled")
        except:
            pass
    
    # Webhook в Make
    if MAKE_WEBHOOK_URL:
        try:
            payload = {
                'event': 'consultation_request',
                'telegram_id': user_data.get('telegram_id'),
                'first_name': first_name,
                'phone_number': user_data.get('phone_number'),
                'segment': user_data.get('segment'),
                'segment_name': user_data.get('segment_name')
            }
            requests.post(MAKE_WEBHOOK_URL, json=payload, timeout=5)
        except:
            pass
    
    # Відправляємо КОНФЕТІ (реакція)
    try:
        await context.bot.set_message_reaction(
            chat_id=query.message.chat_id,
            message_id=query.message.message_id,
            reaction=[ReactionTypeEmoji(emoji="🎉")]
        )
    except:
        pass
    
    text = get_consultation_booked_text(first_name, user_data.get('phone_number'))
    
    await query.edit_message_text(text, parse_mode='HTML')
    
    # Даємо миттєву цінність, поки клієнт чекає
    await asyncio.sleep(1)
    await context.bot.send_message(
        chat_id=query.message.chat_id,
        text=f"""
💡 **{first_name}, поки ви очікуєте дзвінок** (це 5-10 хв), ось чек-лист '3 головні помилки при розлученні':

<b>1. Емоційні рішення:</b> Починати ділити майно чи підписувати документи на емоціях. 
<i>(Результат: втрата активів, про які 'забули').</i>

<b>2. Усні домовленості:</b> Вірити обіцянкам про аліменти/майно 'на словах'. 
<i>(Результат: через рік ніхто нічого не платить, довести неможливо).</i>

<b>3. Затягування:</b> Думати, що 'все само вирішиться', і не фіксувати статус-кво. 
<i>(Результат: супруг/а може вивести активи або набрати боргів, які стануть спільними).</i>

Це допоможе вам підготуватися до розмови з адвокатом.
""",
        parse_mode='HTML'
    )

async def handle_text(update: Update, context: ContextTypes.DEFAULT_TYPE):
    """Обробка текстових повідомлень"""
    await update.message.reply_text(TEXT_UNKNOWN_MESSAGE)

# =====================================================
# НАГАДУВАННЯ (ЗБЕРЕЖЕНО З v3.0)
# =====================================================

def get_quiz_job_name(user_id: int) -> str:
    return f"quiz_reminder_{user_id}"

async def schedule_quiz_reminder(context: ContextTypes.DEFAULT_TYPE, user_id: int, chat_id: int):
    """Планує нагадування про квіз через 15 хвилин"""
    job_name = get_quiz_job_name(user_id)
    
    # Видаляємо старі
    current_jobs = context.job_queue.get_jobs_by_name(job_name)
    if current_jobs:
        for job in current_jobs:
            job.schedule_removal()
            logger.info(f"⏰ Скасовано старе нагадування {job_name}")

    # Ставимо нову
    context.job_queue.run_once(
        quiz_reminder_callback,
        900,  # 15 хвилин
        chat_id=chat_id,
        user_id=user_id,
        name=job_name
    )
    logger.info(f"⏰ Заплановано нагадування через 15 хв")

async def remove_quiz_reminder(context: ContextTypes.DEFAULT_TYPE, user_id: int):
    """Видаляє нагадування про квіз"""
    job_name = get_quiz_job_name(user_id)
    current_jobs = context.job_queue.get_jobs_by_name(job_name)
    if current_jobs:
        for job in current_jobs:
            job.schedule_removal()
        logger.info(f"⏰ Видалено нагадування {job_name}")

async def phone_reminder_callback(context: ContextTypes.DEFAULT_TYPE):
    """Нагадування про номер телефону"""
    job = context.job
    user_id = job.user_id
    
    user_data = context.application.user_data.get(user_id)
    phone_exists = user_data and 'phone_number' in user_data
    
    if phone_exists:
        logger.info(f"⏰ Нагадування скасовано (вже є номер)")
        return
    
    logger.info(f"⏰ ВІДПРАВЛЯЮ нагадування про номер для {user_id}")
    
    from telegram import KeyboardButton, ReplyKeyboardMarkup
    keyboard = [[KeyboardButton("📱 Поділитися номером", request_contact=True)]]
    reply_markup = ReplyKeyboardMarkup(keyboard, one_time_keyboard=True, resize_keyboard=True)

    await context.bot.send_message(
        chat_id=job.chat_id,
        text=TEXT_PHONE_REMINDER,
        parse_mode='HTML',
        reply_markup=reply_markup
    )

async def quiz_reminder_callback(context: ContextTypes.DEFAULT_TYPE):
    """Нагадування про квіз"""
    job = context.job
    user_id = job.user_id
    
    user_data = context.application.user_data.get(user_id, {})
    if 'phone_number' in user_data:
        logger.info(f"⏰ Нагадування скасовано (квіз пройдено)")
        return

    logger.info(f"⏰ ВІДПРАВЛЯЮ нагадування про квіз для {user_id}")
    await context.bot.send_message(
        chat_id=job.chat_id,
        text=TEXT_QUIZ_REMINDER,
        parse_mode='HTML'
    )

# =====================================================
# НОВА ФУНКЦІЯ: НАГАДУВАННЯ ПРО ОФФЕР
# =====================================================

async def offer_reminder_callback(context: ContextTypes.DEFAULT_TYPE):
    """Нагадування, якщо юзер отримав оффер, але не записався"""
    job = context.job
    user_id = job.user_id
    chat_id = job.chat_id
    first_name = job.data  # Отримуємо ім'я з 'data'

    # Перевіряємо user_data, чи не записався вже юзер
    # (Ми додамо 'status' в user_data у функції book_consultation)
    user_data = context.application.user_data.get(user_id, {})
    status = user_data.get('status', 'new')

    if status == 'scheduled':
        logger.info(f"⏰ Нагадування про оффер скасовано (статус 'scheduled') для {user_id}")
        return

    logger.info(f"⏰ ВІДПРАВЛЯЮ нагадування про оффер для {user_id}")
    
    # Текст із твого ж IMPLEMENTATION_CHECKLIST.md
    text = f"""
{first_name}, я бачу ви ще не записалися на консультацію.

Можливо у вас виникли питання?

Напишіть мені - я можу відповісти прямо зараз, 
або допоможу записатися на зручний для вас час.
"""
    
    # Відправляємо текст БЕЗ кнопки, щоб стимулювати відповідь
    await context.bot.send_message(
        chat_id=chat_id,
        text=text,
        parse_mode='HTML'
    )

# =====================================================
# ГОЛОВНА ФУНКЦІЯ
# =====================================================

def main():
    """Запуск бота"""
    
    logger.info("=" * 60)
    logger.info("🤖 ЗАПУСК БОТА v3.1 ULTIMATE IMPROVED")
    logger.info("=" * 60)
    
    # Запускаємо Flask
    flask_thread = threading.Thread(target=run_flask, daemon=True)
    flask_thread.start()
    logger.info("🌐 Flask web-server запущено")
    
    # Створюємо Application
    application = Application.builder().token(BOT_TOKEN).build()
    
    # Реєструємо обробники
    application.add_handler(CommandHandler("start", start))
    application.add_handler(CallbackQueryHandler(question_1, pattern='^start_quiz$'))
    application.add_handler(CallbackQueryHandler(question_2, pattern='^q1_'))
    application.add_handler(CallbackQueryHandler(question_3, pattern='^q2_'))
    application.add_handler(CallbackQueryHandler(question_4, pattern='^q3_'))
    application.add_handler(CallbackQueryHandler(question_5, pattern='^q4_'))
    application.add_handler(CallbackQueryHandler(question_6_phone, pattern='^q5_'))
    application.add_handler(CallbackQueryHandler(book_consultation, pattern='^book_consultation$'))
    application.add_handler(MessageHandler(filters.CONTACT, process_contact))
    application.add_handler(MessageHandler(filters.TEXT & ~filters.COMMAND, handle_text))
    
    logger.info("🚀 Бот v3.1 запущено!")
    logger.info("📊 10 сегментів активовано")
    logger.info("💬 Детальні мині-кейси активовано")
    logger.info("=" * 60)
    
    application.run_polling(allowed_updates=Update.ALL_TYPES)

if __name__ == '__main__':
    main()
